/**
 * Primitivos(imutáveis) -  string, number, boolean, undefined, null (bigint, symbol)
 * 
 * 
 */

// atribuindo um novo valor a variável
let nome = 'Paulo';
nome = 'Ulisses';
console.log(nome);

// copiando o memso valor a duas variáveis
let a = 'A';
let b = a;
console.log(a, b);


// Referência (mutável) - array, object, function

// array
let c = [1, 2, 3];
let d = a;
console.log(c, d);

// object {}